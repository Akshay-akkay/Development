<%- include('includes/head.ejs') %>

    <% if (users.length> 0) { %>
        <h1>This is users page</h1>
        <ul>
            <% users.forEach(user=> { %>
                <h2>
                    => <%= user %>
                </h2>

                <% }) %>
        </ul>

        <% } else { %>
            <h1>No users yet.</h1>
            <% } %>
                <%- include('includes/end.ejs') %>